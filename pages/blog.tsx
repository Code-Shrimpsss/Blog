import { NextPage } from 'next';
import { useRouter } from 'next/router';

const BlogPage: NextPage = () => {
	return (
		<section className="relative h-[26rem] w-full">Developing...</section>
	);
};

export default BlogPage;
